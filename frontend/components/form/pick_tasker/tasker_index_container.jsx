import React from 'react';
import TaskerIndexComponent from './tasker_index_component';

const mapStateToProps = (state, ownProps) => {
    return {

    }
}

const mapDefaultToProps = () => {
    return {

    }
}

export default connect(mapStateToProps, mapDefaultToProps)(TaskerIndex);